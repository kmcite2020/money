part of '../../../features/core/providers.dart';

int totalAmount(String id) {
  // return entriesForPerson(id).where(
  //   (element) {
  //     return element.person.value?.id == id;
  //   },
  // ).fold(
  //   0,
  //   (
  //     previousValue,
  //     eachPersonEntry,
  //   ) {
  //     return (eachPersonEntry.amount) + previousValue;
  //   },
  // );
  return 0;
}
